import Navbar from "../components/Navbar.jsx";

const Header = () => {

    return (
        <div className="fixed justify-center top-0 flex items-center w-full h-[80px]">
            <Navbar />
        </div>
    );
}

export default Header;
